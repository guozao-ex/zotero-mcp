# 复制成 .env 后填写（.env 已被 .gitignore 忽略，不会入库）
# 生成令牌：openssl rand -hex 24
TRANSLATION_TOKEN=
# 来源 IP 白名单（Caddy remote_ip）：默认 private_ranges 只允许私网。
# 从公网访问必须填你自己的出口 IP（curl ifconfig.me 查看），多个用空格分隔：
# ALLOWED_CLIENT_IP=203.0.113.7

# 建议：正式部署时把联系方式写进 UA（上游明确建议，便于站点在异常流量时联系你）。
# 不设则用 compose 里的默认 UA（不带联系方式）。
# USER_AGENT=Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36 zotero-mcp/1.0 (you@example.com)
